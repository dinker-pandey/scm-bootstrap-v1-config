server:
  port: 9091
  
spring:
  profiles: default
eureka:
  instance:
    # enable to register multiple app instances with a random server port
    instance-id: ${spring.application.name}:${random.uuid}

eureka:
  client:
    serviceUrl:
      defaultZone: http://discovery-server:9088/eureka/